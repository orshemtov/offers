import * as React from "react"
import Offer from "../../models/Offer"


type Props = {
  offer: Offer
};

const logo = "logoUrl"

export const OfferForm = ({ offer }: Props) => {
  return (
    <div>

    </div>
  )
}